< a a a a a a a a a a a a a a a a a a d >   
[ aone atwo athree afour afive asix aseven aeight anine aone0 aoneone aonetwo aonethree aonefour aonefive aonesix aoneseven aoneeight class  ] 
four ? one one ? one one two ? ? ? ? ? eight ? one two ? three  
three two ? one ? two ? ? one three ? two three four two two two ? two  
? two two ? two two ? two one four three three ? eight three two two ? three  
three one ? one one ? ? ? one ? three four ? ? three ? two six three  
three ? one one one one one one one ? two four three five ? ? two one two  
two one one ? one one ? two ? three three three ? six three one two ? two  
two ? ? one one ? one two one ? three two three eight two ? one one two  
three ? one one one ? ? two one ? ? ? ? one ? one one one two  
two ? ? one ? ? one two ? ? two two two eight three ? two ? three  
? one one one one one one two ? two ? three ? five three ? one ? three  
? ? two one ? two one two ? three ? four ? five one two two three two  
four ? one one one two one one one three three four three eight three two two two two  
four ? ? one one one ? ? ? two two two ? ? ? one ? one two  
four one ? one one ? ? ? one four ? two four seven three two two ? three  
three two two two two two two two three one ? ? ? eight one two two four four  
two two ? one one ? ? ? one two three three three five three ? two two two  
three ? ? one one ? ? two ? two three three ? two two two ? three two  
two ? one one one two ? ? one ? two ? three five ? one two two two  
? one ? one one ? one two ? ? two ? three two ? ? ? one two  
two two one ? ? ? ? ? ? ? three three ? two two two one one two  
three one one one ? ? one two ? ? ? ? two ? three two two three three  
? ? one ? two ? ? two one ? three three four five three two two four three  
four ? ? ? two two ? two one four three four ? ? two two ? ? two  
? two ? one one one one ? one two two ? two ? one one one one two  
three one one one one one one two two one ? two two eight three one two eight three  
three one one one one two ? ? ? three two ? ? eight three ? two two three  
? one one one ? two ? two one three three four two four three two two ? three  
? ? ? one ? one one two ? three ? ? ? eight three two two one three  
? two one one one one ? two ? two ? two three ? ? ? ? one two  
? ? one one one ? one one ? ? two ? two eight ? two ? one three  
four two one one one two one two one four three three three seven ? two ? ? three  
? one one one one one one two one three two ? ? eight three ? two three three  
? ? ? ? one one ? one ? two three ? two eight ? ? one one two  
two two one one one one one one ? ? ? two ? three one one one one two  
four two one one two two one two one ? three four three four ? ? two two two  
? two two ? two two ? two one three three three three eight ? ? two ? two  
three ? one ? ? two ? ? three one ? four ? five ? ? ? four four  
three ? one ? one ? one ? ? ? ? ? ? four ? ? ? ? three  
two ? one ? one ? ? two one three ? two three four ? two two two three  
two ? one one one one one ? one ? two ? one seven ? ? two two three  
? ? ? ? one two ? two one two two three three ? three one two ? three  
two ? two one ? two ? ? one four ? ? two ? three two ? ? three  
three two two ? ? two ? ? one three ? three ? ? three one two two two  
four two ? one ? one one ? one three ? ? three ? two two ? three two  
? ? two two two ? ? one two ? ? four two four three two two seven four  
two ? one one ? one one two one two ? two two eight ? ? two one three  
three two ? one two one one ? ? ? two three three eight ? ? two ? two  
four one ? one ? one ? ? two one three ? ? ? one two ? one two  
? two two one one two one ? ? two ? three ? five two ? two ? two  
? ? ? one one ? one two ? two ? ? three eight ? ? two ? two  
two one ? one one one one one ? one one ? two ? one ? two one two  
? two one one ? two ? two one two two ? four eight three one two ? three  
? ? one one one one two one ? ? two four three ? two one two one two  
? ? one one two two ? ? one ? three ? ? ? ? one ? one two  
two ? ? ? two ? ? ? one two two ? ? four two ? two one two  
four one one ? ? one one two one two two three ? three three two one ? three  
? ? ? ? one one one two one three ? ? ? eight ? two two ? three  
four two one one two ? ? ? ? two ? ? ? ? three two two four two  
four ? ? ? two two two two one ? three four ? ? three two two eight three  
? two ? ? two two one two one ? ? three three eight three two two two two  
four ? ? ? one one ? two ? ? ? two four seven three two two seven three  
? two ? one two two one ? one ? two ? ? ? one two two ? two  
three ? one one ? two one two one four ? four four two three ? ? three three  
? ? ? one one two one ? one two ? two three two two one one one two  
two ? ? one one one one ? one two ? three three five two ? two ? two  
? ? one one two ? two two one ? two ? four ? three two ? ? three  
? ? ? ? ? one one two one four ? four four five three two two ? three  
? two ? one two ? one one one ? three four three ? two two two ? two  
? ? one one one ? one one one two three two ? three two two one ? three  
? one ? one one one ? two one three two two two ? three two two six three  
three ? one one one two ? ? one ? three three three four three two two ? two  
three one one one ? ? one one one two ? two two five one one two ? three  
four ? ? ? two two one one one one ? three three three ? two ? ? two  
? one one one one one one two one four three ? ? ? ? ? two three three  
? ? ? one one two one one one two ? ? ? ? one one ? one three  
? ? ? ? two ? one two one three three ? three eight ? ? two three two  
four two two ? one one one two one two ? ? ? ? two two two ? two  
three one ? ? ? one one two ? two three three three five three ? ? one two  
one one one one one ? ? ? ? ? one ? one one one one one ? one  
three ? one ? one one one two ? ? two two four eight three ? two three three  
? two one one one ? one two ? three three ? four eight three ? two two three  
four two one ? one ? one one one ? three three ? four two two one one two  
two one one ? ? ? one two one ? ? ? two eight ? ? two five three  
two two two ? two two ? two one three three ? four five ? two two five three  
four one one one one two ? two ? ? three four ? five ? two two two three  
? two one ? two two ? ? ? ? ? four ? ? three ? two one two  
two two one one one ? one two one two three three three five two one two one two  
two ? one one one one one ? ? ? two two ? eight ? one two one two  
two ? ? one ? two one two ? three three four two eight three two ? two three  
three ? one one one one ? two one three two two two eight one one ? ? two  
four two ? ? two two one ? one ? two two ? three ? two two two three  
two two ? one one ? one one ? two ? ? three three two one one ? two  
? two two one two two one two ? two three three ? two two ? one one two  
? one one one ? ? one ? one ? two two three ? ? two two one two  
? ? one ? ? ? ? two ? three two four four ? three two two ? three  
two ? ? ? one one one two one two two four two eight ? two ? three three  
three two ? one ? one one one ? ? two three ? three three one one two two  
? two ? one one ? one ? one ? three ? two four two ? ? ? two  
three two one ? ? one one one one two two three ? five ? one one one two  
three two one one one two one ? one two two four three ? ? two two ? two  
? ? one ? one two one ? ? three three four two eight ? ? two three three  
two two ? ? one one ? two one two two ? three ? three two two two two  
two one one ? one ? ? two one two two two two four one ? ? two two  
three two ? one two ? one one one two two ? three two ? ? ? three two  
four ? one one one one one ? one ? ? two ? four two two ? two two  
two two ? ? one one one two ? four three ? two seven ? two two ? three  
three ? ? one one ? one two one two three ? ? ? two ? ? ? two  
? two one ? one two ? two one ? three four ? eight three ? ? ? three  
three two one ? one ? one one one ? three four two four one one ? one two  
two ? one one one one one two one two three ? three five three one two one two  
? ? ? one ? one one one ? two ? three ? eight three ? one ? two  
four one ? one ? two one ? one ? three four ? ? three two two five three  
? one ? one one ? one one ? two ? two two eight one one one one two  
two one one ? ? two one ? one two two ? two ? ? one ? two three  
three one one one ? ? one two one two two four four two ? ? one one three  
three ? one ? one ? one two ? ? ? four three eight two two two one three  
? one ? ? one one one ? one three ? ? ? three ? two one one three  
four two ? one one two ? ? one two two three three eight ? two two ? two  
two one one one one two ? two one ? two ? ? ? ? one two five three  
? two ? one ? ? one ? ? ? ? four two five three two ? three three  
? ? ? one ? one one one ? one two four three eight two two two ? two  
two one ? one one one ? one one two two two three eight ? ? two ? two  
two two two ? ? ? one ? one ? ? four three eight two ? ? ? two  
? ? ? one one one one one one two ? ? three ? three one ? two two  
four ? ? ? ? two one one one two ? three ? ? two ? ? ? two  
? one ? ? ? ? ? two one four ? two ? ? three two two seven three  
? ? one ? one one one two one ? ? three three ? two one ? one two  
? two one one ? two ? two ? two two ? ? ? two ? two ? two  
two one ? one ? ? ? two ? two two two four eight ? two two two three  
? two one ? ? two ? ? one ? two ? three ? ? ? two ? two  
? two one one one one ? two one four three ? ? eight ? ? two four two  
four two two two two two ? two one ? three four four seven three two two ? three  
four one ? one one ? ? two one ? ? four two ? three ? two six three  
? two one one one one ? two one two three ? three five ? two one ? two  
? ? one ? one ? one two one ? two four four five ? two two one two  
three ? two ? two two one ? one two three three three ? three ? ? seven two  
one one ? one one ? one two ? two two ? one two ? one one two one  
? one one one ? ? one two ? ? three two ? four three two two six three  
two two ? one one one one one one two ? ? ? three three one ? ? two  
three ? ? ? ? ? two one three one one two ? five three one one seven four  
? ? ? one one one one one ? ? ? ? three ? two ? ? one two  
? one one one one one one two one two two four four eight ? one two ? three  
? two ? ? ? two one ? one two three three ? five ? two ? one two  
three ? one ? ? two ? two one ? two four three ? ? two two four three  
two one one ? ? ? one one ? one one one one ? one ? two one two  
two two ? ? one two ? two ? three ? three ? eight ? ? two four three  
two one one one one one one two ? ? ? four two ? ? two ? one two  
two two two one ? two ? two one three three four three four ? two two six two  
